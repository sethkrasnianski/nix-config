;;; config.el -*- lexical-binding: t; -*-

(setq user-full-name "admin"
      user-mail-address "admin@localhost")

(setq doom-theme 'doom-one)

(setq display-line-numbers-type 'relative)

(setq org-directory "~/org/")
